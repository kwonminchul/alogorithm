/*
    isFive

    숫자를 입력받아 5와 같으면 true를,
    그렇지 않으면 false를 리턴해야 합니다
*/

const { reset } = require("colors");

function isFive(num) {
  return num === 5 ? "true" : "false";
  //   if (num === 5) {
  //     return true;
  //   } else {
  //     return false;
  //   }
}
